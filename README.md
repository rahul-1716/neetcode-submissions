# NeetCode Solutions — @rahul-1716

> Synced automatically from [NeetCode.io](https://neetcode.io) · Repository: `neetcode-submissions`

---

## What is this?

[NeetCode.io](https://neetcode.io) is a coding interview preparation platform featuring curated problems, video solutions, and an in-browser code editor. This repository is automatically populated with your accepted (or all) solutions using the **GitHub Sync** feature.

---

## How GitHub Sync works

1. **Connect your GitHub account** on [neetcode.io/profile/github](https://neetcode.io/profile/github).
2. **Auto-commit** — every time you submit a solution on NeetCode, it is pushed here automatically (configurable by status).
3. **Bulk Sync** — push all your past solutions at once from the GitHub settings page.
4. **Manual sync** — from the submission history panel on any problem page, sync or remove individual submissions.

---

## Repository structure

Solutions are organized by topic folder, then problem ID. Each submission is stored as a separate file:

```
<topic-folder>/
  <problem-id>/
    submission-0.<ext>   ← first submission
    submission-1.<ext>   ← second submission
    ...
```

**Example:**
```
Data Structures & Algorithms/two-integer-sum/submission-0.py
Data Structures & Algorithms/binary-search/submission-0.ts
Python For Beginners/python-hello-world/submission-0.py
```

---

## Supported languages

| Language | Extension |
|---|---|
| Python | `.py` |
| JavaScript | `.js` |
| TypeScript | `.ts` |
| Java | `.java` |
| C++ | `.cpp` |
| C# | `.cs` |
| Go | `.go` |
| Rust | `.rs` |
| Kotlin | `.kt` |
| Swift | `.swift` |
| SQL | `.sql` |

---

## Settings

Manage your sync preferences at [neetcode.io/profile/github](https://neetcode.io/profile/github):

- **Auto-commit toggle** — enable or disable automatic commits on submission
- **Status filter** — sync all submissions or accepted only
- **Rename repository** — rename this repo or start fresh with a new one
- **Bulk Sync** — push all past solutions at once (rate-limited)
- -Why tf is neetcode down

---

*Generated by [NeetCode GitHub Integration](https://neetcode.io)*


graph TD
    %% Title
    title[CODE CRAFT — System Architecture]
    style title fill:none,stroke:none,font-weight:bold,font-size:18px

    %% Client Tier
    subgraph CLIENT_TIER [CLIENT TIER]
        Browser["Browser <br> (Next.js 15 App Router | React Components | Monaco Editor | Tailwind CSS)"]
    end
    style CLIENT_TIER fill:#dbeafe,stroke:#2563eb,stroke-width:2px

    %% Backend Tier
    subgraph BACKEND_TIER [BACKEND / BaaS TIER]
        direction LR
        ConvexDB["Convex Real-Time Database"]
        ConvexFunc["Convex Server Functions <br> (Queries, Mutations)"]
        Clerk["Clerk Authentication"]
    end
    style BACKEND_TIER fill:#dcfce7,stroke:#16a34a,stroke-width:2px

    %% External Services Tier
    subgraph EXTERNAL_TIER [EXTERNAL SERVICES TIER]
        direction LR
        Piston["Piston Execution API"]
        Lemon["Lemon Squeezy Payment Gateway"]
        Vercel["Vercel (Deployment & CDN)"]
    end
    style EXTERNAL_TIER fill:#ffedd5,stroke:#ea580c,stroke-width:2px

    %% Vertical Interconnections
    Browser <===>|"HTTP Requests / Server Actions"| BACKEND_TIER
    BACKEND_TIER <===>|"Webhooks / API Calls"| EXTERNAL_TIER
    Browser -.->|"Hosting & Edge Routing"| Vercel


    erDiagram
    users {
        string _id PK
        string userId
        string email
        string name
        boolean isPro
    }
    codeExecutions {
        string _id PK
        string userId FK
        string language
        string code
        string output
    }
    snippets {
        string _id PK
        string userId FK
        string title
        string language
        string code
        string userName
    }
    snippetLikes {
        string _id PK
        string snippetId FK
        string userId FK
    }
    snippetComments {
        string _id PK
        string snippetId FK
        string userId FK
        string content
        string userName
    }

    users ||--o{ codeExecutions : "executes"
    users ||--o{ snippets : "owns"
    users ||--o{ snippetLikes : "likes"
    users ||--o{ snippetComments : "writes"
    snippets ||--o{ snippetLikes : "receives"
    snippets ||--o{ snippetComments : "has"

    graph TD
    System(("CODE CRAFT SYSTEM"))
    
    User["User (Developer)"]
    Clerk["Clerk Authentication Service"]
    Piston["Piston Code Execution API"]
    Lemon["Lemon Squeezy Payment Gateway"]

    %% Data Flows
    User -->|"Code Input, Login Request, Snippet Data"| System
    System -->|"Execution Output, Snippet Results, Profile Stats"| User
    
    System <-->|"Auth Tokens, User Sync Webhooks"| Clerk
    System <-->|"Code + Language / stdout + stderr"| Piston
    System <-->|"Checkout Request / order_created Webhook"| Lemon

    style System fill:#f3e8ff,stroke:#7e22ce,stroke-width:3px

    graph TD
    %% External Entities
    User["User"]
    Clerk["Clerk Auth Service"]
    Piston["Piston API"]
    Lemon["Lemon Squeezy"]

    %% Processes
    P1(("1.0 User Authentication"))
    P2(("2.0 Code Editing & Execution"))
    P3(("3.0 Snippet Management"))
    P4(("4.0 Profile & Analytics"))
    P5(("5.0 Payment Processing"))

    %% Data Stores
    D1[("D1: Users")]
    D2[("D2: Snippets & Executions")]
    D3[("D3: Payment Records")]

    %% Flows
    User --> P1
    P1 --> D1
    P1 <--> Clerk

    User --> P2
    P2 --> D2
    P2 <--> Piston
    D2 --> P4

    User --> P3
    P3 --> D2

    User --> P5
    P5 --> D3
    P5 <--> Lemon

    style D1 fill:#fef08a,stroke:#ca8a04
    style D2 fill:#fef08a,stroke:#ca8a04
    style D3 fill:#fef08a,stroke:#ca8a04

    graph LR
    %% Actors
    Guest("Guest User")
    AuthUser("Authenticated User")
    ProUser("Pro User")

    subgraph Boundary ["CODE CRAFT — Editor Module"]
        UC1(["Select Language"])
        UC2(["Choose Theme"])
        UC3(["Adjust Font Size"])
        UC4(["Write Code"])
        UC5(["View Output"])
        UC6(["Run Code"])
        UC7(["Execute Non-JS Languages"])
        UC8(["Share Snippet"])
    end

    %% Connections
    Guest --> UC1 & UC2 & UC3 & UC4 & UC5
    
    AuthUser --> UC1 & UC2 & UC3 & UC4 & UC5
    AuthUser --> UC6
    AuthUser --> UC8

    ProUser --> UC7
    UC7 -.->|"<<extend>>"| UC6

    graph LR
    AuthUser("Authenticated User")

    subgraph Boundary ["CODE CRAFT — Snippet Module"]
        UC1(["Browse Snippets"])
        UC2(["Search by Title"])
        UC3(["Filter by Language"])
        UC4(["View Snippet Detail"])
        UC5(["Copy Code"])
        UC6(["Star / Unstar Snippet"])
        UC7(["Post Comment"])
        UC8(["Delete Own Snippet"])

        %% Includes
        UC4 -.->|"<<include>>"| UC5
        UC4 -.->|"<<include>>"| UC7
    end

    AuthUser --> UC1 & UC2 & UC3 & UC4 & UC6 & UC8

    graph LR
    AuthUser("Authenticated User")

    subgraph Boundary ["CODE CRAFT — Profile Module"]
        UC1(["View Execution Count"])
        UC2(["View Most-Used Language"])
        UC3(["View Total Snippets Created"])
        UC4(["View Total Stars Received"])
        UC5(["Browse Execution History"])
        UC6(["Paginate Execution History"])
        UC7(["View Starred Snippets Tab"])

        %% Includes
        UC5 -.->|"<<include>>"| UC6
    end

    AuthUser --> UC1 & UC2 & UC3 & UC4 & UC5 & UC7

    graph LR
    FreeUser("Free User")
    ProUser("Pro User")
    Lemon(("Lemon Squeezy <br> (System Actor)"))

    subgraph Boundary ["CODE CRAFT — Pricing & Payment Module"]
        UC1(["View Pricing Page"])
        UC2(["Compare Free vs Pro Features"])
        UC3(["Click Upgrade to Pro"])
        UC4(["Lemon Squeezy Checkout Opens"])
        UC5(["Process Payment"])
        UC6(["Send order_created Webhook"])
        UC7(["Update isPro = true in Convex"])
        UC8(["Unlock All 10 Languages"])
    end

    FreeUser --> UC1 & UC2 & UC3
    UC3 --> UC4
    Lemon --> UC5 & UC6
    UC6 --> UC7
    ProUser --> UC8

    flowchart TD
    Start([START]) --> V["User visits CodeCraft URL"]
    V --> R["Clerk Auth Component Renders"]
    R --> Choice{"Has existing session?"}
    
    %% YES Path
    Choice -- YES --> L["Load user from Convex D1"]
    L --> Dashboard["Render authenticated dashboard"]
    Dashboard --> End1([END])
    
    %% NO Path
    Choice -- NO --> Page["Display Clerk Sign-In Page"]
    Page --> OAuth["User chooses OAuth (Google/GitHub) or Email"]
    OAuth --> Verify["Clerk verifies credentials"]
    Verify --> JWT["JWT issued & session created"]
    JWT --> Webhook["Svix Webhook fires to /api/webhooks/clerk"]
    Webhook --> Mutation["Convex createUser mutation → users table"]
    Mutation --> Redirect["Redirect to CodeCraft dashboard"]
    Redirect --> End2([END])

    flowchart TD
    Start([START]) --> Write["User writes code in Monaco Editor"]
    Write --> Click["Clicks Run Button"]
    Click --> CheckJS{"Is language JavaScript?"}
    
    %% JS Path
    CheckJS -- YES --> CallAPI["Call /api/execute endpoint"]
    
    %% Non-JS Path
    CheckJS -- NO --> CheckPro{"Is user Pro?"}
    CheckPro -- YES --> CallAPI
    CheckPro -- NO --> Modal["Show 'Upgrade to Pro' Modal"] --> End1([END])
    
    %% API Actions
    CallAPI --> PostPiston["POST to Piston API: {language, version, code}"]
    PostPiston --> Status{"Execution Successful?"}
    
    %% Success Path
    Status -- YES --> Out1["Return stdout to client"]
    Out1 --> Out2["Display output in Output Panel"]
    Out2 --> Store["Store in Convex codeExecutions table"]
    Store --> Zustand["Update Zustand store state"] --> End2([END])
    
    %% Error Path
    Status -- NO --> Err1["Return stderr"]
    Err1 --> Err2["Display error in Output Panel (red)"] --> End3([END])

    flowchart TD
    Start([START]) --> Mon["User has code in Monaco Editor"]
    Mon --> Click["Clicks 'Share' button in header"]
    Click --> Modal["ShareSnippetDialog modal opens"]
    Modal --> Title["User types snippet title"]
    Title --> CheckEmpty{"Title is empty?"}
    
    CheckEmpty -- YES --> Err["Show validation error: 'Title required'"] --> Title
    
    CheckEmpty -- NO --> Save["User clicks Save button"]
    Save --> Mutate["Convex createSnippet mutation fires"]
    Mutate --> WriteDB["Data written to snippets table: title, language, code, userId, userName"]
    WriteDB --> Reactive["Convex reactive query updates all subscribed clients"]
    Reactive --> UI["New snippet appears on Community Snippets page in real-time"]
    UI --> Close["Modal closes"] --> End([END])

    flowchart TD
    Start([START]) --> Page["User visits Pricing Page"]
    Page --> Click["Clicks 'Upgrade to Pro' button"]
    Click --> Comp["UpgradeButton component fires"]
    Comp --> Overlay["Lemon Squeezy checkout overlay opens"]
    Overlay --> Card["User enters card/payment details"]
    Card --> Process["Lemon Squeezy processes payment"]
    Process --> Success{"Payment Successful?"}
    
    %% Failure Path
    Success -- NO --> Err["Show payment error to user"] --> End1([END])
    
    %% Success Path
    Success -- YES --> Webhook["Lemon Squeezy sends POST to /api/lemon-squeezy webhook"]
    Webhook --> Sig{"Webhook signature valid?"}
    
    Sig -- NO --> AuthErr["Return HTTP 401 Unauthorized"] --> End2([END])
    
    Sig -- YES --> Extract["Extract userId from webhook meta.custom_data"]
    Extract --> Convex["Call Convex updateUserProStatus mutation"]
    Convex --> DB["Set isPro = true in users table"]
    DB --> Refresh["User refreshes — Pro features unlocked"]
    Refresh --> Unlock["All 10 languages now available"] --> End3([END])
